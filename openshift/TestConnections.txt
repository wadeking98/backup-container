# Uses the 'testConnection' script from https://github.com/BCDevOps/openshift-developer-tools
# Example:
# echo -e "backup-postgres"  | xargs -I {} testConnection -n devex-von-image-dev -f TestConnections.txt -p {}

google.com:80
issuer-kit-wallet:5432
